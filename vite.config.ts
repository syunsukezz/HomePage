import { defineConfig } from 'vite'
//import viteCompression from 'vite-plugin-compression'

export default defineConfig({
  plugins: [
    // コメントアウトまたは削除: viteCompression({ algorithm: 'brotliCompress', ext: '.br' })
  ],
})